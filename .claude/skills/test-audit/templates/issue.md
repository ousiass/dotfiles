## 概要

<1-2行: 何が不足していて、その結果どんな退行を検知できないか>

## 軸 / カテゴリ

<テスト / カバレッジ / CI> — <テスト不在 / 品質 / 閾値 / ゲート欠落 / 無効化>

## 現状

- **該当箇所**: `<ファイルパス>:<行番号>`（存在しない場合は「該当設定なし」）
- **検出内容**:
  ```
  <該当コード・設定の引用、または実測値>
  ```

## リスク

<この穴があることで何が起きるか。例: テストが落ちてもマージできるため、認証バイパスの退行が本番まで到達しうる>

## 重大度

<🔴 重大 / 🟠 重要 / 🟡 注意 / 🟢 軽微> — <判定理由>

## 対応方針

<具体的な手順。CI 系は適用可能な設定断片を添える>

```yaml
# 例: 追加するジョブ
```

## 完了条件

- [ ] <検証可能な条件。例: PR で `coverage` ジョブが走り、閾値未達で fail する>
- [ ] <例: `unit-test` が required status check に含まれる>

## 関連

- 消化には `/impl`（E2E 追加なら `/e2e-turbo`）を使う
- 監査元: `/test-audit`
