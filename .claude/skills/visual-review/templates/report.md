# Visual Review Report

> 生成日時: YYYY-MM-DD HH:MM
> 対象プロジェクト: <プロジェクト名>
> baseUrl: <撮影対象の URL>
> ブランチ: <ブランチ名>
> デザイン基盤: <検出されたデザインシステム（例: Tailwind CSS, tokens.ts）>
> 撮影マトリクス: <ビューポート> × <テーマ>

## サマリー

| 指標 | 値 |
|-----|-----|
| 撮影対象画面数 | N |
| 撮影枚数（実装） | N |
| モック対応あり | N 画面 |
| 撮影失敗 | N |
| 検出した指摘数 | N |
| 重大度 🔴 | N |
| 重大度 🟠 | N |
| 重大度 🟡 | N |
| 重大度 🟢 | N |

## レビュー範囲

| パス | 対象 | 状態 |
|-----|-----|-----|
| 第1パス（デザイン品質） | 全画面 desktop × light | 完了 / 一部 |
| 第2パス（モック照合） | モックあり N 画面 | 完了 / 一部 / 対象なし |
| 第3パス（レスポンシブ） | N 画面 mobile | 完了 / 一部 |
| 第4パス（ダークテーマ） | N 画面 dark | 完了 / 一部 / 対象なし |
| tablet | N 画面 | 確認 / 未確認 |

**未確認の範囲**: <レビューしていない画面・ビューポート・テーマを明記する>

## 指摘一覧

### 🔴 重大

#### 1. <指摘のタイトル>
- **画面**: <画面名>（`<URL>`）
- **条件**: <ビューポート> / <テーマ>
- **スクリーンショット**: `<outDir>/shots/<file>.png`
- **現象**: <スクリーンショットで確認できた事実>
- **期待**: <あるべき状態>
- **カテゴリ**: 表示崩れ / レスポンシブ / 視覚的一貫性 / アクセシビリティ / 状態表現
- **推奨アクション**: <具体的な修正方針>

### 🟠 重要

...

### 🟡 提案

...

### 🟢 軽微

...

## モック乖離

### 🔴 / 🟠 / 🟡 / 🟢

#### 1. <乖離のタイトル>
- **画面**: <画面名>
- **モック**: `<モックのパス>`（種別: HTML / 画像 / Markdown）
  - スクリーンショット: `<outDir>/shots/<id>__mock__<viewport>.png`（HTML モック時）
- **実装**: `<outDir>/shots/<id>__<viewport>__<theme>.png`
- **差分**: <モックと実描画の具体的な差>
- **カテゴリ**: レイアウト構造 / 要素の有無 / 配置 / デザイン値 / 文言 / 状態表現
- **推奨アクション**: 実装をモックに合わせる / モックを更新する / 要確認

## 仕様乖離

| 種別 | 対象 | 内容 |
|-----|-----|-----|
| 未実装 | `<画面名>` | 仕様書 / モックにあるが対応ルートが存在しない |
| 仕様未記載 | `<URL>` | 実装されているが仕様書の画面一覧に無い |
| モック未整備 | `<URL>` | 実装された画面に対応するモックが無い |
| モック取り残し | `<モックパス>` | モックがあるが対応実装が無い |
| story 欠落 | `<コンポーネント>` | 仕様書に定義された状態の story が無い |
| 到達不能 | `<URL>` | 実装はあるが導線が無く URL 直打ち以外で到達できない |

## 撮影失敗

| 画面 | 条件 | エラー | 判定 |
|-----|-----|-------|-----|
| `<画面名>` | desktop / light | `<エラーメッセージ>` | 🔴 実装不具合の可能性 |

## 確認済み画面

- [x] `<画面名>` — 指摘 N 件（desktop/light, mobile/light）
- [x] `<画面名>` — 指摘なし（desktop/light）
- ...

## 前提

（`--auto` 実行時のみ）自動判断で採用した前提を列挙する。

- 動的セグメント `[id]` に `1` を使用
- モック `mocks/xxx.html` を画面 `<画面名>` に対応付け
