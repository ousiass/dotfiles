# 構成テンプレート

用途ごとの本編アウトライン。**ドキュメントの目次をそのまま写さない。**
仕様書の1章が0ページになることも、1章が5ページに分かれることもある。

## 共通ルール

- **1ページ1主張。** h2 がそのページの結論。
- **Part 構成にする。** 本編は 4 パート前後、各パート 4〜8 ページ。パートの切り替わりに `v-dark` の章扉を挟む。
- **`v-dark` は資料全体の句読点。** 連続させない。章扉・結論・1つの数値を見せるページだけ。
- **各ページに presenter note を付ける。** `<!-- ... -->` で「このページで固定したい1点」を書く。
- **数値には出典と年を添える。** 裏が取れない数値は載せず、README の「未確定事項」へ。
- **ページ番号を必ず出す。** `global-bottom.vue` が担保する。消さない（詳細は SKILL.md）。

## わかりやすさの規律

構成が正しくてもページが読めなければ意味がない。全ページで守る。

### 視線の順序

上から `v-kicker`（分類）→ `h2`（タイトル）→ 図 / カード → 結論バナー → `v-note` と読む。
**結論はページ下部にある**ため、図を見てから結論を読む順序になる。
**この順に読めば理解できる状態にする。** 下にあるものを読まないと上が理解できない構成にしない。

### 意味のない強調をしない

色を変える・反転させる・太くするのは、**読み手に「ここは他と違う」と伝えるとき**だけ。
装飾として強調しない。

| 強調してよい | 強調してはいけない |
| --- | --- |
| 提案する案（他は比較対象） | 並列に並ぶ要素の最後の1枚 |
| Before / After の After | 単に締めくくりのカード |
| 比較表の自社の行 | 表の最終行 |
| 4フェーズのうち今回の対象 | 順番が最後のフェーズ |

- **`.accent` は既定で使わない。** 付ける理由を presenter note に1行で書けないなら外す
- 付けるとしても**1ページ1枚**。2枚以上あるとどちらが主役か読めない
- `--v-magenta` 系は**資料全体で1〜2箇所**。ここぞという1ページのために取っておく
- 太字は1ページ2〜3語まで。段落まるごと太字にしない

### カードの色は情報ではない

カードの地・枠・上端の帯は**区切りを示す構造**であって、意味を持たない。

- カードごとに色を変えない。種類の違いは見出しの語と並び順で示す
- 「重要度が高いから濃い色」のような色のグラデーションを作らない
- 図の中で色を意味に使う場合（例: 自社の範囲を囲う）は、そのページ内で凡例か
  ラベルによって何を示す色か分かるようにする

### 色だけで区別しない

`.accent` のカードには必ず `index` / `ordinal` / 見出しの語でも差が付くようにする。
色覚特性や白黒印刷でも情報が落ちない状態にする。比較表の `●` `—` も同様（色ではなく記号で区別する）。

### 図と本文で同じことを言わない

図で示した関係を、下の `v-note` で文章化しない。図が読めていない前提の説明を足すくらいなら、図を直す。
`v-formula-banner` は図の要約ではなく、**図から導かれる結論**を書く場所。

### 文字量の上限

密度プリセット（`references/tone.md`）に従う。標準なら:

| 要素 | 上限 |
| --- | --- |
| h2（タイトル） | 20字・1行まで |
| カード内の本文 | 60字 |
| 箇条書き | 5行、1行30字 |
| `v-note` | 80字・2行まで |
| 1ページの総文字数 | 400字 |

超えたら、書き足すのではなく**削るかページを分ける**。

### 読める文字サイズ

`visual.css` の最小は `v-note` の 0.68rem 相当。**これより小さいサイズを新規に作らない。**
投影時に後方の席から読めなくなる。表が入らない場合は文字を縮めず、列を減らすか Appendix へ回す。

### 用語

- 専門用語・略語は初出のページで1度だけ定義する（`v-note` に1行で足りる）
- 同じ概念を別の語で言い換えない。資料内で語を1つに固定する
- 読み手の業界で通じない社内用語は、通じる語に置き換える

### 数値

- 単位は `<span class="unit">` に分け、数字本体を大きく見せる
- 桁区切りを入れる（`1,233`）。同じページ内で桁表記を混ぜない
- 比較する数値は単位と基準年を揃える。揃わないなら並べない

## h2 はタイトル

h2 は**そのページが何のページかを示すタイトル**。主張文を入れない。

| 良い（タイトル） | 悪い（主張文が入っている） |
| --- | --- |
| 申請経路の現状と導入後 | 申請を出したあと、社員は待つ以外にできることがない |
| 申請から記録までの4段階 | 申請から記録まで、4つの段階を同じ基盤で扱う |
| 4つの手段の比較 | 個別に作るのではなく、共通の土台に載せる |
| 現状を示す3つの指標 | 承認までの日数と、差し戻しの手戻りが減る |

- **10〜20字**。2行になったら長い
- 体言止め・名詞句でよい。むしろその方がタイトルとして読みやすい
- 「〜について」「〜の話」は不要。「比較」「4段階」のように中身を名指しする
- ページを開いた瞬間に「何を見せられるのか」がわかることが条件

`v-kicker`（`Comparison` `Before / After` 等）は英語のセクションラベル。
h2 の日本語タイトルと役割が重複しないよう、kicker は分類、h2 は具体を書く。

## 主張はどこに書くか

h2 から主張を外したぶん、**そのページで言いたいことは別の場所が担う**。上から順に検討する。

1. **カードや図そのものに語らせる** — `v-duo` の giant（いま / 導入後）、
   `v-two-stack` の h3、`v-audience` の h3。これで伝わるなら何も足さない
2. **`v-formula-banner`** — 1 で足りないとき、ページ最下部に結論を1文で置く。
   図を見たあとに読む位置なので、「図から導かれる結論」を書く
3. **`v-lead`（リード文）** — **本当に必要なときだけ。** 既定では置かない。
   図より先に前提を伝えないと図が読めない場合に限る（文章が2行入るぶん図の領域が狭くなる）

**同じページに `v-formula-banner` と `v-note` を両方置かない。** どちらか一方にする。

主張は h2 に書かないが、**捨てるわけではない**。`OUTLINE.md` の「主張」列に必ず書き、
そのページがその主張に到達しているかを 3-2 の通し確認で判定する。

## アウトラインの検証

本文を書く前に「# / セクション / h2 の文 / 使うパターン / 出典」の表を作る。
書き始める前に次を確認する。

1. **「主張」列を縦に読む。** 接続が飛ぶ箇所があれば、そこにページが足りないか順序が違う。
   h2（タイトル）の列は章立てが読み取れるかを見る
2. **同じことを2回言っていないか。** 言っていたら片方を Appendix へ
3. **ゴールに向かっているか。** 用途ごとのゴール（下記）に到達しないページは削る
4. **`v-dark` が2枚続いていないか**
5. **パターンが3ページ連続で同じでないか。** 同じ形が続くと読み手の集中が切れる

## A. 技術説明・設計共有

**ゴール:** 読み手が「この設計で作ってよい」と判断できる。

| # | セクション | 内容 | パターン |
| --- | --- | --- | --- |
| 1 | 表紙 | プロダクト名と一文の定義 | `v-cover` |
| 2 | 課題（dark） | 現状の何が重いのか | `v-statement` |
| 3 | 現状の分解 | 自作・既存構成だと何工程あるか | `v-pipeline` / `v-fragment` |
| 4 | Part 1 何を作るか | 提供対象 / 提供内容 / 新しさ | `v-audience` |
| 5-7 | 主要機能 | 機能を3〜5枚で並べる | `v-detail-grid` |
| 8 | 全体像 | レイヤー構成図と提供範囲 | `v-arch` |
| 9 | 処理の流れ | リクエストからレスポンスまで | `v-flow-detail` |
| 10 | 連携 | 既存システムとの接続 | `v-bridge` / `v-hub` |
| 11 | Part 2 なぜこの設計か | 技術選定の対比 | `v-duo` / `v-usecase-grid` |
| 12 | 比較 | 他手段との機能マトリクス | `v-matrix` |
| 13 | 非機能 | 性能・可用性・セキュリティ | `v-detail-columns` |
| 14 | Part 3 導入 | 導入ステップ | `v-process` |
| 15 | ロードマップ | フェーズごとの範囲 | `v-phase-grid` |
| 16 | 結論（dark） | 一文で言い切る | `v-statement` |

タイトルと主張の対応（実例）:

```
p3  自作構成の5つの処理     ← 主張: 個別に組むと毎日止めずに回すのが難しい
p5  全体のレイヤー構成       ← 主張: どこまでを自分たちが提供するか
p6  リクエストから応答まで   ← 主張: 判定と配信が1つの経路でつながる
p9  4手段の機能比較          ← 主張: 4条件を同時に満たすのはこの構成だけ
p11 導入までの4ステップ      ← 主張: 既存環境を作り替えずに始められる
```

Mermaid は状態遷移・シーケンスなど CSS で組むのが不合理な図に限る。
構成図・レイヤー図・フロー図は `v-arch` / `v-flow-detail` の方が資料に馴染む。

## B. 提案・営業プレゼン

**ゴール:** 読み手が「話を先に進める」と判断できる。

| # | セクション | 内容 | パターン |
| --- | --- | --- | --- |
| 1 | 表紙 | 提案の一文をサブコピーに | `v-cover` |
| 2 | Part 1 何の提案か | 誰に / 何を / 何が新しいか | `v-audience` |
| 3 | 提供価値 | 相手が得るものを並列で | `v-detail-grid` |
| 4 | Before / After | 現状の不便と導入後 | `v-duo` |
| 5 | 体験（image） | キービジュアル1枚 | `v-image-slide` + `v-image-copy` |
| 6 | Part 2 なぜ成立するか（dark） | 課題の本質 | `v-statement` |
| 7 | 立場ごとの困りごと | ステークホルダー別 | `v-usecase-grid` |
| 8 | 既存の選択肢との違い | 4カテゴリの比較 | `v-data-table` / `v-matrix` |
| 9 | 市場規模 | 数値3点 + 補足2点 | `v-metrics` + `v-two-stack` |
| 10 | いま参入する理由 | タイミングの根拠 | `v-event-strip` |
| 11 | Part 3 どう提供するか | 提供形態 | `v-plans` |
| 12 | 導入ステップ | 契約から稼働まで | `v-process` |
| 13 | 収益・費用 | 収益源または費用構造 | `v-stream-grid` / `v-split` |
| 14 | ロードマップ | フェーズ | `v-phase-grid` |
| 15 | Final position（dark） | 提案の到達点 | `v-statement` |
| 16 | Closing（image） | 締めのメッセージ | `v-end` + `v-end-copy` |

タイトルと主張の対応（実例）:

```
p3  申請手続きを1つの基盤に      ← 主張: 種類ごとの手続きを1つにまとめる
p4  申請経路の現状と導入後        ← 主張: 出したあと待つ以外にできることがない状態をなくす
p5  申請から記録までの4段階       ← 主張: 追加時に変えるのは設定だけ
p6  4つの手段の比較              ← 主張: 個別開発ではなく共通の土台に載せる
p7  現状を示す3つの指標           ← 主張: 滞留も手戻りも仕組みで減らせる
```

金額・料率を確定できない場合は「個別提示」と書き、資料には載せない。

## C. 進捗・成果報告

**ゴール:** 読み手が「次に何を判断・承認すべきか」を持ち帰る。

| # | セクション | 内容 | パターン |
| --- | --- | --- | --- |
| 1 | 表紙 | 対象期間を `v-cover-info` に | `v-cover` |
| 2 | サマリー（dark） | 期間の結論を一文で | `v-statement` |
| 3 | 主要指標 | 数値3点 | `v-metrics` |
| 4 | 指標の内訳 | 補足の数値帯 | `v-event-strip` |
| 5-7 | やったこと | 施策ごとに1ページ | `v-detail-grid` / `v-process` |
| 8 | 計画との差分 | 予定と実績の対比 | `v-duo` / `v-data-table` |
| 9 | 課題 | 残っている問題 | `v-usecase-grid` |
| 10 | 次のアクション | 期日つき | `v-timeline` / `v-phase-grid` |
| 11 | 依頼事項（dark） | 判断してほしいこと | `v-statement` |

報告は結論が先。悪い数値を後半に隠さず、3ページ目の `v-metrics` に並べる。

## Appendix

本編は「読み手が判断するのに要る分」だけにし、残りは Appendix に置く。
Appendix があることで本編を削れる。**本編を短くするための場所であって、余り物置きではない。**

### 何を落とすか

| 本編に残す | Appendix へ |
| --- | --- |
| 判断に要る結論と根拠1点 | 根拠の詳細、算出過程、前提条件 |
| 主要な数値3点 | 内訳、時系列、感度分析 |
| 全体構成図 | サブシステムごとの構成、API 一覧、データモデル |
| 比較の結論 | 競合ごとの詳細評価 |
| フェーズの見出し | フェーズごとのタスク一覧、体制、KPI |
| — | 会社概要、体制、契約条件、セキュリティ論点 |

質問されたときに開くページが Appendix にある状態を目指す。

### 章分けと番号

10ページを超えるなら章に分ける。章ごとに `v-appendix-cover`（`v-dark`）の扉を置く。

| 用途 | 章の分け方 | 番号 |
| --- | --- | --- |
| 技術説明 | 設計詳細 / 運用 | `D-1…` `O-1…` |
| 提案 | ビジネス / テクニカル | `B-1…` `T-1…` |
| 報告 | 数値詳細 / 実施ログ | `M-1…` `L-1…` |

各ページの kicker に番号と英語ラベルを入れる。口頭で「Appendix B-7 を見てください」と言える状態にする。

```html
<div class="v-kicker">Appendix B-7 / Revenue streams</div>

## 9つの収益源
```

### 配置

`pages/appendix.md` に置き、`slides.md` 末尾の `---\nsrc: ./pages/appendix.md\n---` で読み込む。
章が増えたら `pages/appendix-business.md` `pages/appendix-technical.md` に分け、`src:` を並べる。
ページ番号は本編から通しで振られる（Appendix 側で番号を振り直さない）。

Appendix のページも本編と同じパターン・同じデザインルールで組む。
密度は上げてよい（`v-data-table-dense` / `v-grid-compact`）が、1ページ1主張は崩さない。

## 収まらないときの対処

| 症状 | 対処 |
| --- | --- |
| カードが3行以上で溢れる | `v-grid-compact` を併用、または項目を減らしてページを分ける |
| 表の行が多い | `v-data-table-dense`。それでも溢れるなら Appendix へ |
| 本文が長い | 文を削る。ページを分けるより、書かないことを決める |
| 図が複雑 | 1ページ1関係に分解する。1枚に2つの関係を描かない |
| 本編が 35 ページを超える | 判断に要らないページを Appendix へ移す |
